"""Step 8 — one self-contained HTML QC report.

Panels: quality score distribution, rejected examples with failure reasons,
duplicate cluster examples, class balance before/after curation, results table.
"""

from __future__ import annotations


def run_report(cfg: dict) -> None:
    """TODO(claude-code): jinja2 template, images inlined as base64 so the file
    is portable. This is the artefact a non-engineer actually looks at."""
    raise NotImplementedError
